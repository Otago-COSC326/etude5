package etude5

import org.neo4j.graphdb.Node

import scala.collection.mutable.ListBuffer

/**No
 * Created by tinhtooaung on 14/05/15.
 */
class BalanceMatchesGenerator(strips: List[Strip]) {

  def findBalanceMatches(length: Int): Unit ={
//    val bestLength = length/2
//    val nonLength = length - bestLength
//    val result = ListBuffer.empty[Node]
//    val bestMatchesGenerator: BestMatchesGenerator = new BestMatchesGenerator(dupStrips)
//    result ++= bestMatchesGenerator.getBestMatches(bestLength)
//    println("working on non")
//    result ++= new NonMatchesGenerator(nonDupStrips).getNonMatches(nonLength)
//    bestMatchesGenerator.printNodes(result.toList)
  }
}
